# contract-cli API Call Skill Disabled

`contract-cli api call` 是预留调试入口，当前不对外开放使用。

保留本目录下的历史参考资料，仅用于后续重新开放时恢复上下文；由于本目录不再包含 `SKILL.md`，通用 `skills` installer 和 `contract-cli skills install` 不应把它作为可用 skill 安装。

当前用户应使用已开放的结构化命令：

- `contract-cli contract ...`
- `contract-cli mdm vendor ...`
- `contract-cli mdm legal ...`
- `contract-cli mdm fields ...`
