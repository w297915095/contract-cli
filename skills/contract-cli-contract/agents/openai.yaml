interface:
  display_name: "Contract CLI Contract"
  short_description: "Run contract commands with field references"
  default_prompt: "Use $contract-cli-contract to run contract search/get/create/template/file/action commands and assemble request JSON from the bundled field references."

policy:
  allow_implicit_invocation: true
